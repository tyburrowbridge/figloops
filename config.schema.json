{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "figloops.config.json",
  "type": "object",
  "required": ["devServer", "viewport", "figma", "routes"],
  "properties": {
    "devServer": {
      "type": "object",
      "required": ["url"],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Base URL of the running dev server (e.g., http://localhost:3000)."
        },
        "waitFor": {
          "description": "Playwright wait condition used after page.goto(). One of: networkidle, load, domcontentloaded.",
          "oneOf": [
            { "const": "networkidle" },
            { "const": "load" },
            { "const": "domcontentloaded" }
          ]
        }
      }
    },
    "viewport": {
      "type": "object",
      "required": ["width", "height"],
      "properties": {
        "width":  { "type": "integer", "minimum": 200 },
        "height": { "type": "integer", "minimum": 200 }
      }
    },
    "figma": {
      "type": "object",
      "required": ["fileKey", "changelogPageName"],
      "properties": {
        "fileKey": {
          "type": "string",
          "minLength": 1,
          "description": "Figma file key — the path segment after /file/ in your Figma file URL."
        },
        "changelogPageName": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the Figma page where per-round changelog entries will be written."
        }
      }
    },
    "routes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["label", "path"],
        "properties": {
          "label": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable name shown in the Figma frame title (e.g., 'Login', 'Dashboard')."
          },
          "path": {
            "type": "string",
            "pattern": "^/",
            "description": "Path appended to devServer.url for this route. Must start with /."
          },
          "waitFor": {
            "type": "string",
            "description": "Optional CSS selector to wait for before capturing this route. Used as page.waitForSelector(<this string>). For wait conditions, use devServer.waitFor."
          }
        }
      }
    },
    "scenarios": {
      "type": "array",
      "description": "Optional interaction-driven captures (modals, drawers, toasts, results/search states, themed variants) that aren't reachable by URL alone. Each scenario navigates to `path`, runs each `setup` step in order, then captures.",
      "items": {
        "type": "object",
        "required": ["label", "path"],
        "properties": {
          "label": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable name for the Figma frame (e.g., 'Sign up modal', 'Dashboard — empty state')."
          },
          "path": {
            "type": "string",
            "pattern": "^/",
            "description": "Path to navigate to before running setup steps."
          },
          "setup": {
            "type": "array",
            "description": "Steps run in order before capture. A bare string is a click; object forms drive inputs (fill/press/select) — e.g. type a value then click Search to reach a results state. Each step auto-waits up to 10s for the selector to be actionable.",
            "items": {
              "oneOf": [
                { "type": "string", "minLength": 1, "description": "Selector to click (shorthand)." },
                {
                  "type": "object",
                  "required": ["action", "selector"],
                  "properties": {
                    "action": { "const": "click" },
                    "selector": { "type": "string", "minLength": 1 }
                  },
                  "additionalProperties": false
                },
                {
                  "type": "object",
                  "required": ["action", "selector", "value"],
                  "properties": {
                    "action": { "const": "fill" },
                    "selector": { "type": "string", "minLength": 1 },
                    "value": { "type": "string", "description": "Text to type into the input." }
                  },
                  "additionalProperties": false
                },
                {
                  "type": "object",
                  "required": ["action", "selector", "key"],
                  "properties": {
                    "action": { "const": "press" },
                    "selector": { "type": "string", "minLength": 1 },
                    "key": { "type": "string", "minLength": 1, "description": "Key to press, e.g. 'Enter'." }
                  },
                  "additionalProperties": false
                },
                {
                  "type": "object",
                  "required": ["action", "selector", "value"],
                  "properties": {
                    "action": { "const": "select" },
                    "selector": { "type": "string", "minLength": 1 },
                    "value": { "type": "string", "description": "Option value to select." }
                  },
                  "additionalProperties": false
                }
              ]
            }
          },
          "waitFor": {
            "type": "string",
            "description": "Optional CSS selector to wait for after setup steps, before capturing. For results/search states, use a selector that only exists once results render."
          }
        }
      }
    },
    "git": {
      "type": "object",
      "description": "Optional git workflow integration. Omit entirely to disable git suggestions.",
      "properties": {
        "branchPerRound": {
          "description": "How the implement gate should handle branching. 'ask' prompts each round, 'always' auto-creates figloops/round-N-<date>, 'never' skips git entirely.",
          "enum": ["ask", "always", "never"],
          "default": "ask"
        }
      }
    }
  }
}
